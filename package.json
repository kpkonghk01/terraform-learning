{
  "name": "terraform-learning",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:kpkonghk01/terraform-learning.git",
  "author": "Pan <kpkong@hk01.com>",
  "license": "MIT",
  "scripts": {
    "build": "scripts/build.sh",
    "test": "jest --clearCache --coverage",
    "package": "mkdir -p dist/layers/deps-layer && cp package*.json dist/layers/deps-layer/ && cp yarn.lock dist/layers/deps-layer/ && cd dist/layers/deps-layer && yarn --frozen-lockfile"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.13",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
